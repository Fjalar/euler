// 40C20 = 40! / (20! * (40 - 20)!) = 40! / (20! * 20!)

// 4C2 = 4! / (2! * 2!) = (4 * 3 * 2 * 1) / ((2 * 1) * (2 * 1)) = (4 * 3) / (2 * 1)

fn main() {
    let mut answer = 1f64;

    for i in 0..20 {
        answer *= (40.0 - i as f64) / (20.0 - i as f64);
    }

    println!("{}", answer);
}
